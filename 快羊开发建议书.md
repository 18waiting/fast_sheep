# 快羊开发建议书

> 日期：2026 年 9 月 18 日  
> 定位：围绕已明确的产品目标，收敛开发范围、简化开发流程、加快可用功能交付。  
> 边界：本文不是代码审计报告，不判断尚未核实的实现情况，也不推测线上与线下的差异。所有内容均为建议，不自动替代项目现有规则或真实店铺操作授权。

## 一、先把产品目标固定下来

快羊要做的是一个**安静运行、AI 优先、必要时人工接管的拼多多多店铺客服客户端**。

用户的核心使用过程应当是：

1. 打开客户端，登录并管理多个拼多多店铺。
2. 系统在后台接收消息。
3. AI 利用必要的接口和知识检索生成回复，在允许的范围内自动处理。
4. 正常处理时，前端只显示轻量状态和提示；用户点开后才查看详情。
5. AI 无法处理、缺少必要信息或处理失败时，明确提醒人工接管。
6. 人工能够看到上下文、完成回复，并确认问题已经处理。

**原始拼多多客服工作台不应成为快羊的主要使用界面。** 如果接入方式需要保留它，可以将其放在登录、排障或兜底入口，而不是要求用户日常围着它操作。

判断一个功能是否应当进入当前开发范围，可以只问：

> 它是否直接帮助用户完成“登录店铺 → 收到消息 → AI 回复 → 必要时人工接管”这个闭环？

如果不能，就先不作为当前阶段的优先事项。

## 二、核心判断：产品交互可以简单，但关键保障不能省略

你的产品设想在用户交互上是清晰、简洁的，没有必要为了实现它先建设一个庞大的通用 Agent 平台。

但“收到消息后调用 AI”与“能够放心地替商家回复”之间，仍需要处理几个明确的问题：店铺身份、重复消息、人工接管、发送结果，以及 AI 没有依据时如何退出。

因此，建议不是把所有复杂性都删掉，而是做两个区分：

- **产品层面做减法：** 界面简单、范围有限、主流程明确。
- **实现层面保留底线：** 不串店、不重复回复、不在人工接管后继续抢答、不把未知结果当成功。

针对你描述的开发体验，最值得调整的是：

> **减少等待、交接和重复分析，而不是减少验证。**

开发中出现问题、修复后再验证，本身是正常过程。普通问题每次都必须重新开启一轮大范围审计和审批，则不应成为默认工作方式。

## 三、先做减法：当前版本只围绕三个使用区域

以下是功能分组建议，不要求推倒现有界面重做。

| 使用区域 | 用户需要完成的事情 | 设计重点 |
| --- | --- | --- |
| 店铺管理 | 登录、查看连接状态、开启或暂停 AI | 区分“店铺已连接”和“AI 已启用”，避免误判 |
| 运行概览 | 了解是否正常运行、是否有待处理问题 | 正常时安静，异常时明确，不逐条弹窗打扰 |
| 会话详情与人工待办 | 查看上下文、了解转人工原因、人工回复 | 提示可以直接进入对应店铺和会话，操作后有明确结果 |

### 提示策略

- 正常 AI 回复：轻量计数、状态或活动记录即可。
- 需要人工：提供可见、可点击、不会轻易丢失的待办。
- 连接失效或持续失败：明确显示系统无法正常服务，不能继续营造“一切正常”的感觉。

**“前端安静”不等于“故障静默”。**

### 建议暂缓的工作

除非已经成为当前闭环的明确阻碍，否则暂不优先投入：

- 把原始客服工作台完整搬到快羊前台。
- 通用多平台框架和与当前需求无关的扩展能力。
- 复杂的多 Agent 编排。
- 大而全的统计报表、管理后台与装饰性页面。
- 自动退款、改价等超出“客服消息回复”的业务动作。
- 为了架构整齐而进行的大范围重写。

已有模块如果不妨碍交付，不必为了“做减法”专门拆掉。**先停止扩张，比立刻清理所有历史代码更重要。**

## 四、按用户能验收的闭环开发，而不是按技术层逐层建设

不建议以“先完成全部底层，再完成全部 AI，最后做前端”作为主要推进方式。建议每次交付一段能够实际演示的完整行为。

下面是验证顺序，不是要求从头重做；已经完成且有验证依据的部分直接复用。

### 第一步：一条消息能正确进入系统

验收重点：

- 消息属于哪个店铺、哪个会话是明确的。
- 用户能够在详情里看到它。
- 同一消息重复到达，不会生成重复处理任务。

### 第二步：能够生成有依据的回复草稿

验收重点：

- 使用正确店铺的上下文和知识。
- 有依据时生成草稿；依据不足时不编造答案。
- 可以验证生成结果，但此阶段不要求自动发送。

### 第三步：完成一次受控发送

验收重点：

- 在明确授权的测试范围内，由人工确认发送。
- 发送对象与草稿所属店铺、会话一致。
- 系统能区分成功、失败和结果未知，而不是仅凭一次调用就显示成功。

人工确认是验证阶段的过渡措施，**不是把最终产品改成人工逐条审核工具**。

### 第四步：AI 无法处理时，人工真的能接住

验收重点：

- 失败或无法回答会产生人工待办，而不只是写日志。
- 提示能够打开正确会话，并显示转人工原因。
- 人工接管后，该会话尚未发出的 AI 回复不再继续发送。
- 人工处理完成后，待办状态可以正确更新。

### 第五步：接入第二家店铺，验证隔离

多店铺是目标能力，应在扩大自动回复范围前验证，而不是最后才补。

验收重点：

- 两家店铺同时接收消息时，身份、知识、待办和发送目标不混用。
- 一家店铺暂停 AI，不应影响另一家。
- 单店连接异常能够被正确识别和提示。

### 第六步：开放有限范围的自动回复

只对已经验证过、范围明确的场景开放自动回复，其余进入人工待办。

不要仅以模型自报“有信心”作为自动发送条件。场景是否允许、依据是否充分、会话是否已被人工接管，都应参与判断。

**最终目标仍然是 AI 自动处理为主，而不是让商家长期逐条确认。**

## 五、简化开发流程：一个任务内完成调查、实现、测试和修复

建议把默认开发流程收敛为：

> 明确一个可验收结果 → 必要的局部调查 → 连续实现与测试 → 修复发现的问题 → 交付验证结果。

其中，定位问题、阅读相关代码、调试、补测试、修复普通失败，原则上都属于同一个任务，不应每发生一次就要求你重新批准。

### 按风险决定检查深度

| 工作类型 | 建议方式 |
| --- | --- |
| 小范围界面调整、已知缺陷修复 | 直接实现，执行相关检查，交付结果 |
| 跨模块但目标明确的功能 | 先给简短计划，再连续实现、测试和修复 |
| 店铺身份、凭证、发送机制、数据迁移变更 | 先做针对性设计检查，明确影响和验证方法 |
| 真实账号操作、真实消息发送、生产发布 | 获得明确授权，并限定操作范围 |

这里要区分两种权限：

- **开发自主权：** 在批准范围内自行读代码、修改、运行测试、修复问题。
- **实际业务操作权：** 登录或操作真实账号、向真实客户发送消息、修改生产数据、发布上线。

扩大前者，不等于自动授予后者。

### 什么时候应停下来询问你

建议只在以下情况中断正常开发：

1. 必须改变已确认的产品行为或验收目标。
2. 必须扩大权限，或者改变身份、安全、真实发送边界。
3. 涉及破坏性数据变更，或不可逆操作。
4. 缺少凭证、环境或关键产品决策，确实无法继续。

普通测试失败、实现细节调整、已知范围内的缺陷，不应自动升级为新的审计任务。

如果现有项目规则要求每一步都经过审批，应先由你确认一次流程简化，再更新相关规则。**不要绕过现有规则，也不要为了修改规则再建设一套更重的审批体系。**

## 六、把重复审计的问题，变成可重复运行的验证

对于每次都会担心的同一个问题，优先建立回归检查，而不是反复写文字分析。

建议保留以下最低保障：

| 关键问题 | 应验证的结果 |
| --- | --- |
| 店铺隔离 | A 店消息不会使用 B 店知识，也不会发往 B 店会话 |
| 重复处理 | 同一消息重复到达，不会产生重复回复 |
| 人工接管 | 接管后，尚未发送的 AI 回复被拦截 |
| AI 无法完成 | 缺少依据、超时或出错时，进入人工待办 |
| 发送不确定 | 结果未知时不盲目重发，先核实或交给人工 |
| 敏感信息 | 凭证和客户敏感内容不会被不必要地暴露在日志或界面中 |

验证可以分层进行：

- 开发时，用模拟消息、固定样例和可控失败场景快速回归。
- 涉及跨模块行为时，跑完整链路检查。
- 涉及真实平台交互时，在明确授权后进行受控验证。

模拟测试通过不代表真实接入已经可用；真实接入有问题，也不意味着每次都要重审整个项目。先根据失败证据缩小范围。

同样，自动化测试不能替代所有人工判断。对于授权、安全边界和实际业务效果，仍需有针对性地检查。

## 七、AI 编程任务要围绕结果，而不是围绕“先审计一下”

建议每个任务只明确四件事：

1. **目标：** 用户完成后能做什么。
2. **范围：** 允许改什么，不改什么。
3. **验收：** 用什么行为或测试判断完成。
4. **停止条件：** 哪些变化必须重新询问你。

不要把每个任务都变成“分析整个项目，给出方案，等待确认”。调查应服务于当前任务，而不是成为独立的长期产物。

### 可直接复用的任务模板

```text
任务：补齐“AI 无法处理时进入人工待办”的完整行为。
如果现有实现已满足其中某项，验证后复用，不重复建设。

目标：
AI 无法回答或处理失败后，用户能看到提示，打开正确会话，
了解原因，并人工完成回复。

范围：
复用现有架构，不增加新框架，不进行无关重构。
不改变真实发送权限，不执行未经授权的真实账号操作。

验收：
1. 缺少依据、AI 超时或处理失败时，产生人工待办。
2. 提示能打开正确店铺和会话。
3. 人工接管后，阻止该会话尚未发送的 AI 回复。
4. 人工处理完成后，待办状态正确更新。
5. 有可重复执行的检查覆盖上述行为。

执行方式：
在本任务内完成必要调查、实现、测试和修复。
普通测试失败请自行定位并继续修复，不要反复停下来申请审计。
如果遇到环境阻塞，说明已完成部分、阻塞证据与需要我提供的内容。

仅在改变产品目标、扩大权限、破坏性数据变更，
或需要执行真实账号操作时，停下来询问我。

交付：
说明用户新增了什么能力、修改了什么、执行了哪些验证、
还有哪些未验证或未解决的问题。不要把未验证写成已完成。
```

这个模板是任务表达方式的示例，不代表已经确认“人工待办”就是当前唯一缺口。实际应选择现有闭环中第一个尚未完成或尚未验证的环节。

## 八、接下来怎么落地

建议只做三件事，不另起一轮大规划：

1. **确认产品范围。** 把本文第一节作为方向约束，后续需求先判断是否服务于核心闭环。
2. **一次性简化开发约定。** 保留安全与真实操作授权，但允许 AI 在单个开发任务内连续调查、实现、测试和修复。
3. **交付一个缺失的完整环节。** 根据现有证据选定下一段闭环，完成后演示、验证，再进入下一段。

不要求为了采用这些建议，先迁移技术栈、整理所有文档、删除所有历史模块或重写全部计划。

对参考项目，也建议只借鉴当前缺口所需要的实现方式和测试场景，不因为它看起来更直接，就整体替换快羊现有架构。

以后每轮开发结束，优先看这三个问题：

- 商家现在比上一轮多完成了哪件事？
- 这件事有什么验证依据？
- 距离完整闭环还缺哪一个具体环节？

不要以新增了多少文档、拆出了多少任务、完成了多少轮审计作为主要进度指标。

---

## 总结

快羊的方向不需要再变复杂：**多店铺登录、后台收消息、AI 自动回复、安静提示、人工兜底。**

开发方式也应围绕这个目标收敛：

> **不重写、不扩张；按完整行为交付，在一个任务内持续实现和验证。把必要保障放进系统，把重复检查变成测试，把你的注意力留给产品决策和真实业务授权。**
