// Clean-room implementation. Derived only from public/project behavioral specifications
// and frozen contracts. Do not consult original proprietary source/binaries.

/** Canonical JSON Schema draft for all clean-room contracts. */
export const CONTRACT_SCHEMA_VERSION = "2020-12";

/** ADR-002 worker RPC protocol version (envelope `version` field). */
export const WORKER_RPC_VERSION = 1;

/** TASK-014 frozen golden fixture version (immutable). */
export const PARITY_FIXTURE_VERSION = "2026-08-15.1";

/** Contract package version. */
export const CONTRACTS_PACKAGE_VERSION = "0.0.0";
