# Repository cleanup and platform-session retirement gate — 2026-09-23

> Evidence report only. It does not change the North Star, PDD MVP, reviewed Roadmap, locked decisions, execution authorization or SHEEP-305 closure. Local workspace root replaces the historical `E:\fast_sheep\` notation. No real platform operation was performed.

## Task and result

- task: Owner-requested repository cleanup, **not a new SHEEP task**
- result: **PARTIAL** — safe documentation/state cleanup performed; untracked evidence de-duplication and platform-session retirement deliberately deferred
- phase/milestone: `MVP_A_FOUNDATION` / `M-A.2 Scene and Context Foundation` (unchanged)
- objective: remove misleading current-state projections and an unreferenced tracked delivery archive; map the dependent platform path and its proof gates before any runtime deletion
- completed_requirements: 3 (state projection + regression guard; README replacement; tracked archive removal)
- total_requirements: 5 (also evidence de-duplication and validated platform replacement)
- next_stage_not_executed: true

## Confirmed findings and changes

1. `project/PROJECT_STATE.json` records SHEEP-305 Lane A `SEALED/PASS`, Lane B offline `SEALED/PASS`, but the prior top-level current-task projections and `sheep_305.status` described the whole task as `NOT_STARTED`. The projections are corrected, while `last_closed_task=SHEEP-304`, `next_authoritative_roadmap_id=SHEEP-305`, all canonical authorization booleans, the detailed lane evidence and historical Roadmap baseline remain unchanged. **CONFIRMED**. The reviewed Roadmap's original task-definition `Status` is not current lifecycle truth.
2. `scripts/validate-project-state.mjs` now rejects a current whole-task `NOT_STARTED` projection when a current lane is `SEALED` with Controller `PASS`; `tests/project-state-consistency.test.mjs` covers the negative case and treats the Roadmap baseline as historical. The test no longer hard-codes a long-obsolete SHEEP-300 → 301 current pointer, and resolves Windows-style Roadmap paths on the local host. **CONFIRMED**.
3. `README.md` described an M0 `fastwork-rebuild` skeleton and stated Electron and platform adapters did not exist. It has been replaced by a navigation-oriented Fast Sheep README; the canonical documents retain their existing ownership. **CONFIRMED**.
4. `ecommerce-ai-architecture.zip` was a tracked 5.4 MB, 83-entry diagram delivery archive (SHA-256 `ba7b2ab6d0d036d29ef8f39a0d4c652bcee1a76f8c7bcc696846f99b0`). Repository text search found no filename reference. Removed from the working tree; Git history can restore it. **CONFIRMED** no static reference; **INFERRED** not required at runtime. Do not rewrite Git history merely to reclaim historical bytes.
5. `engineering-plan.md` and `recommended-solution.md` each exactly match an untracked report copy by SHA-256. The three pre-existing untracked report trees contain evidence and links and have no Git restore guarantee. Neither the tracked roots nor the untracked trees were removed or staged. **CONFIRMED** duplication; **DEFERRED** single-copy selection until evidence retention and link destinations are approved. `建议` and the provenance-bearing `快羊开发建议书.md` were also retained.

## Platform-session dependency and replacement gates — no code deletion

Current path (**CONFIRMED** by static code inspection):

`app-shell.ts` selected Shop → `platform-surface.ts` visual bounds / `onBoundsChange` → `workbench-store.ts` → typed `platformSetViewBounds` command → `platform-session-coordinator.ts` → PDD session → `pdd-view-host.ts` `WebContentsView` visibility and bounds. Main PDD session/service also owns login-required status, document lifecycle, ingress and shop/session ownership. The renderer does not own the seller DOM. The embedded page remains login/session, Transport, diagnostic and human fallback infrastructure under the North Star and DEC-024/031.

**Candidate replacement, not an approved implementation:** keep Main-owned PDD session and Transport, but expose the visible seller view only behind an explicit login/diagnostic/human-fallback entry rather than reserving the daily workspace. The new entry must provide an explicit active-Shop target and Main-owned show/hide/bounds lifecycle; closing or switching shops must not leave a stale `WebContentsView` on screen. A fully headless replacement is **PRODUCT_DECISION_REQUIRED** and has no evidence here.

Do not remove the current platform surface, PDD view/session/adapter, store/identity models, migrations, `resources/worker`, or other platform packages yet. To authorize later retirement, prove in separate scoped work:

1. **Offline negative/integration tests:** view hide/show and shop switch do not leak or overlay another Shop; session and document-generation ownership survive fallback entry/exit; login-required/error/unsupported states remain visible; ingress/dedupe/view and manual fallback remain reachable; stop/dispose has no stale callback; no AI, transport send or policy activation is introduced.
2. **Build and packaging:** desktop typecheck/build, PDD-focused tests and relevant smoke, typed IPC contract and electron-builder `resources/worker` inclusion remain valid.
3. **Controlled real evidence only with recorded authorization:** a designated controlled logged-in session proves login/reauth, inbound observation, own platform identity provenance and fallback. Current `SHEEP-305` identity-source observation is `NOT_EXECUTED_NO_LIVE_SESSION`; it does **not** supply this proof. No real binding write, policy activation, AI or send is authorized by this cleanup.
4. **Review gate:** Owner/Controller approve fallback UX, identity-binding provenance storage and specific runtime scope. Only then perform a reversible UI change, validate, and separately consider old module retirement. No historical task ID reuse or automatic SHEEP-306 start.

## Boundaries and outstanding items

- security/tenant/secret/data-model/AI-disclosure/audit-traceability changes: **none** in runtime code; state text and read-only validation only
- third-party/license changes: none; diagram archive removed from current working tree, provenance remains in Git
- `PRODUCT_DECISION_REQUIRED`: duplicate-report retention location; any headless platform-session architecture or fallback UX change; binding provenance storage and field-level SHIPPING_TIME semantics remain separate existing gates
- `BLOCKED`: controlled real-session proof is unavailable in this cleanup; no live session attached and no authorization widened
- `DEFERRED`: M10/M11/other-platform package retirement; README/package metadata rename; Git history compression
- reference-tree modifications: none; external reference trees were not accessed
- modified files: `README.md`, `project/PROJECT_STATE.json`, `scripts/validate-project-state.mjs`, `tests/project-state-consistency.test.mjs`; deleted tracked archive: `ecommerce-ai-architecture.zip`; added this report
- tests and smoke: see final verification note below

## Final verification note

- `node --test tests/project-state-consistency.test.mjs`: **PASS 20/20**, including the accepted-lane/whole-task-negative case and Roadmap baseline case.
- `node scripts/validate-project-state.mjs`: **PROJECT_STATE_CONSISTENCY=PASS**.
- `python3 -m json.tool project/PROJECT_STATE.json`: PASS; `git diff --check`: PASS.
- README local links: **8/8 present**; tracked archive absent and `resources/worker` preserved; 17 files in the three pre-existing untracked report trees preserved.
- Runtime integration/smoke and full `pnpm` suite: **NOT_RUN** (no runtime code changed; this local host has no installed workspace `node_modules`). Static dependency mapping is not real-session verification. No real platform/AI/send operation occurred.
- Controller review: **NOT_RUN**. This maintenance report cannot close SHEEP-305 or authorize the next stage.
