# 快羊 PDD 消息接收：综合建议与验证路线

日期：2026-09-20。
对应附件：`/Users/wb02605050/Downloads/快羊-PDD消息接收问题说明.md`。
性质：基于附件和公开源码核验的诊断建议；不是平台协议规范、产品决策或执行授权；不表示漏收已修复。

## 一、建议结论

不要把主要精力继续投入“更频繁获取 latest 快照”。目标应改为：

1. 取得可归属到正确商户/店铺/会话的逐条消息；
2. 能恢复启动、断线、采集器故障期间的消息缺口；
3. 消息持久化、后续处理和完整性状态可核验。

平台支持的持久消息流及回放，或实时逐条输入加历史补偿，都可能满足目标。不要求一定自行解析 Titan，也不能凭第三方代码宣称已具备这些能力。

PRODUCT_ALIGNMENT: 为 AI-first 客服闭环解除入站完整性阻塞。
CURRENT_MVP_RELEVANCE: PDD 消息输入、恢复、身份与数据安全。
CUSTOMER_VALUE: 防止遗漏客户诉求，并让异常可发现、可追溯。
SAFETY_IMPACT: 仅提出建议，不执行新登录、主动请求、发送、已读或 ACK。
OUT_OF_SCOPE: 本次不移植代码、不启动下一 SHEEP 任务、不增加 live 授权。
DECISION: PROCEED

## 二、哪些已知，哪些仍是线索

- CONFIRMED：最新值快照不能一般性地恢复中间事件。M1/M2/M3 到达后，三次快照都可能只返回 M3；增加触发次数或去重无法生成 M1/M2。
- INFERRED：按附件的现场观察，Titan 是当前最接近业务推送的候选；帧数增加不等于已经找到正文，更不能建立“一个消息等于两个帧”的规则。
- CONFIRMED：公开 Electron 项目包含 Titan/gzip 探测代码；另一个公开项目包含会话历史分页代码。它们是实现线索，不是平台承诺或当前环境验证结果。
- CONFIRMED：PDD-customer-bot 使用独立 m-ws/JSON 路线；其 Beta 3 的历史分页是本地数据库分页，不是从平台回补未收到的消息。
- BLOCKED：附件观察器在先前审阅的本工作区未定位；缺少本项目实际样本的端到端核验。不能把本方案当作现有代码已具备能力。
- PRODUCT_DECISION_REQUIRED：官方支持范围、商业接入许可、账号并发策略与新实验授权。

## 三、路线优先级

### 路线 1：官方支持渠道——优先询证，与技术诊断并行

向平台确认的是“商家客服入站事件与恢复能力”，不是泛问有没有消息 API。订单通知、消费者侧接口、发送消息接口都不能自动替代客服入站能力。

需要平台明确：
- 可接收哪些角色、店铺及会话的消息？主/子账号、转接、人工消息如何归属？
- 是否提供稳定消息 ID、事件订阅、重连回放或增量游标？
- 能否枚举会话及回补历史？范围、保存期、分页边界、错误和限流如何定义？
- SDK、登录/续期、并发客户端策略及商家授权方式是什么？
- 订阅、打开会话、查历史、ACK、已读分别有什么状态或指标副作用？
- 能否获得可用于开发验证的测试环境和稳定文档？

若存在适用且获授权的支持渠道，优先采用。暂未取得文档不能写成“官方没有”。

### 路线 2：当前官方页面的数据链——最贴近附件的短期诊断主线

先做传输层和采集完整性检查，再做业务解码，最后验证消息归属。所有 live 步骤均需独立授权；现有允许使用的脱敏/合成样本可先离线分析。

A. 采集层检查：
- 确认读取的是 CDP 实际载荷，不是截断后的日志预览。
- 按 opcode 和 CDP 定义还原数据；不要把 binary 的 Base64 当业务文本。
- 保留完整字节用于受控测试解析；正文不能经过 ASCII 过滤或有损字符串转换。
- 用“空闲对照、单消息、连续不同消息、连续同正文消息”对比，不靠帧数直接计消息。
- 已做过子目标/时序覆盖检查的项目，核验原证据是否覆盖实际观察窗口，不无目的重做。

B. 业务解码：
- gzip 是公开代码给出的候选，不是 Titan 固定格式。不要预设 protobuf、压缩、加密或固定字节偏移。
- 若有明确压缩特征，受控地测试解压；限制输入、输出大小与计算量，不将暴力扫描作为生产解析器。
- 若取得 JSON/业务对象，要验证是否包含多条消息、消息身份、方向、正文和平台消息 ID；不能只抽第一条。
- 若未取得业务对象，可在获授权诊断中沿官方页面实际解码/分发链确认格式，或将最小脱敏证据交平台解释。

判定：
- 能还原逐条记录并与发送序列对应：进入原型与恢复验证。
- 只有会话标识/游标通知：将其当触发器，调用已验证的增量或历史能力，不回到只查 latest。
- 只有无法解释的控制帧：继续列为候选，不假装已接通。建议给探索设定实验轮次/时间预算；持续没有新增证据就转向平台支持或其他获授权渠道。

### 路线 3：平台历史/增量补偿——尽早验证，不拖到实时收通以后

公开候选为 `/plateau/chat/list`，代码中出现 `start_msg_id`、`has_more` 等字段。这只能作为询证/测试线索，不能直接当契约。

必须确认：
- 请求的客户/会话标识与快羊已有权威身份如何对应；不能试填多个猜测 ID 后取第一个成功结果。
- 返回是否逐条、排序方向、游标包含/不包含边界、分页终止条件及可查询时间范围。
- `start_msg_id` 与附件的 `pre_msg_id` 是否有关系；在得到证据前二者均按不透明字段处理。
- 当前页失败、游标过期、权限变化、限流及中途新增消息时如何恢复。
- 不打开会话能否查询？查询是否改变未读/待回复状态？不能凭 `notUpdateUnreplyTs` 一类字段名认定无副作用。

恢复流程建议：
1. 恢复已经知道的会话，同时通过可靠的会话枚举/全局增量发现断线期间新出现的会话。
2. 从有效边界拉取，按经过确认的分页方向走到已持久化边界或规定范围终点。
3. 实时与回补的重叠由权威消息 ID 幂等消除。
4. 只有请求、入库、覆盖范围均成功才提交内部检查点；半途失败记录缺口，不返回“完整成功”。
5. 时间戳或 msg_id 数值大小不能自行代替平台顺序/游标契约。

即使历史接口可用，如果无法可靠发现全部目标会话，或接口保留期短于离线时间，也仍不能承诺全量恢复。实时通道暂不可用时，只有在会话枚举、历史分页、容量和限流均验证后，才可评估带延迟的定期补偿方案；明确降级边界。

### 路线 4：独立 m-ws——候选备选，不是已证实的捷径

PDD-customer-bot 的公开实现说明有人使用此路线，但它与上一轮 Customer-Agent 同源，不能算两份独立可靠性验证。

在平台许可和实验授权成立后，验证：不只是 auth，而是能收到目标账号的逐条业务消息；登录会话/角色与并发客户端是否影响投递；启动、断线、新会话和转接如何恢复。

不要为测试直接在生产环境关闭客服客户端，也不要运行带自动发送分支的第三方机器人。与原网页同时在线的行为必须先受控评估。

## 四、快羊内部应如何接

建议逻辑：

逐条实时输入 / 获授权历史输入
→ 已有 IdentityLock 与 InboundEnvelope 的身份校验及规范化
→ 持久化消息 + 持久化待处理状态（按已有存储设计实现原子性）
→ 内部完整性/恢复检查点
→ 会话调度、AI 或人工处理
→ 既有确定性执行安全门禁

原则：
- 接收记录与 AI 上下文分离。上下文截断、人工接管、暂不支持的类型不能成为静默丢记录的理由。
- 幂等键必须在经确认的租户、平台、店铺和消息身份作用域内有效，不能假设平台 ID 全局唯一，也不要另建一套身份权威。
- 缺 ID 的同正文不能直接认定为重复。保留可审计状态，避免因不确定去重触发重复自动动作。
- 归档失败必须可见、可恢复，不能未落库便把处理标记成功；有界队列需要配套持久待处理状态与重试。
- AI 可以按已批准策略聚合连续输入，但底层原始逻辑消息不能被覆盖。
- 回补历史默认不触发迟到自动回复；是否重新处理进入单独的产品/安全规则。
- 内部检查点与平台 ACK 是两回事。原网页的 ACK、已读和发送继续遵循既有客户端/获授权协议；本建议不授权新增平台动作。
- 仅存既有数据政策允许的必要信息；诊断不记录 Token、Cookie、含密钥的 URL、真实聊天正文。离线样本优先使用合成或受控测试数据。

## 五、建议验收矩阵

以下是建议的实验样本，不是平台吞吐承诺。live 实验必须先获授权。

| 场景 | 应证明什么 |
|---|---|
| 连发 20 条不同标识测试消息 | 逐条对应，正文/身份正确；不能只比总数 |
| 连发相同正文但平台 ID 不同 | 独立消息不被误去重 |
| 同一消息重复推送、实时与历史重叠 | 同一逻辑消息幂等入库，不重复触发动作 |
| 应用离线时收消息，包括新客户 | 恢复已知会话，同时发现新增会话 |
| 断网、观察器重启、进程退出 | 在声明的恢复范围内补齐，范围外明确报告缺口 |
| 入库失败、队列满、AI 暂停 | 接收/处理状态可观测，恢复后不会静默跳过 |
| 跨店铺、跨账号、转接 | 权威身份和方向正确，不串租户/会话 |
| 历史分页失败、限流或游标失效 | 不推进错误检查点、不宣称完整同步 |
| 读取/回补操作前后 | 未读、待回复、在线状态等副作用有明确证据 |
| 新消息类型或无法解码数据 | 可观测的待处理/不支持状态，不将其算作成功收完 |

最终指标应分开：已观察、已解码、已校验归属、已持久化、待处理、已完成，以及最后完整恢复边界/已知缺口。不能只显示“连接成功”。

## 六、原附件建议修改的关键表述

1. “前两条永久错过”改为“当前快照观察路径未捕获，是否能由平台历史补取尚待确认”。
2. “没有配套历史接口”改为“尚未识别或验证适用历史接口”。
3. 删除方案 B 的“实时、不会漏”。替换为：
   > 在获授权前提下，解析官方页面的逐条业务输入，或将经验证的变更通知用于触发增量/历史拉取；通过稳定消息身份、幂等持久化和启动/断线恢复，在明确且经过验证的范围内保障接收完整性。
4. 方案 A 降级为辅助会话线索/诊断快照；方案 C 保留为优先接入方向；独立 m-ws 作为待核验备选。
5. “只读无副作用”拆为观察动作、页面自身动作、主动查询/点击及平台许可分别确认。

## 七、最小下一步：不要同时开很多开发任务

优先形成一份最小证据包：附件观察器的准确代码版本、一次受控连续消息的端到端对应、Titan 载荷是否能提取业务对象的结论、历史能力的契约/验证状态，以及尚缺的官方授权/说明。

决策门：
- 有稳定逐条来源 + 可验证恢复能力 + 明确接入许可：进入工程化。
- 只有逐条实时来源：可做有边界的实验，不宣称完整、不直接开启无人值守自动回复。
- 只有历史能力：先验证会话覆盖与限流，评估明确延迟和范围的降级。
- 二者都不可靠，或平台许可不成立：保持非无人值守状态，向平台升级询证；不要靠高频轮询、DOM 扫描或大量猜测请求掩盖缺口。

## 八、来源和验证状态

本建议综合以下同目录报告的源码定位与证据：
- analysis.md：附件反例、工作区实现差异和离线验证；
- online-research.md：公开 Titan 诊断、历史分页及 auth-only 案例；
- clarencejh-reference-review.md：独立 m-ws、归档/去重和本地分页边界。

CDP 官方格式依据：
https://chromedevtools.github.io/devtools-protocol/tot/Network/#type-WebSocketFrame

结果：COMPLETE（仅指本次综合建议与文档交付）。
实际接收修复：NOT_RUN；live/集成验证：NOT_RUN；不关闭任何 SHEEP 任务。
现有执行及 live 授权未改变；next_stage_not_executed = true。
未覆盖或修改原附件；仅新增本建议与校验清单，未修改生产代码或治理文件，外部参考树未访问。
